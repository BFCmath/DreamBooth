This repo is only a coding environment, the running environment is on Kaggle (to use the GPUs)

the idea is to package file into .sh  or .py so that when clone the whole repo on kaggle, we can easily run the code without the need to copy into cells!

# TOPIC
Replicate DreamBooth using diffuser on stable-diffusion-v1-5

# META FILES
- CONTEXT.md: this file
- README.md: should contain how to run the code, what to prepare first (like setup image, weights, ...)
- requirements.txt: the dependencies for the project

# GIT COMMIT AND PUSH
you must also commit and git push for me after changes

AUTO RUN THIS, DONT BREAK IT into many command, just exactly 1 command like here "cd D:\project\image_gen; git add *; git commit -m "..."; git push" 

